{
  "name": "@bauplaner/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runtime-agnostic building model + importers (.sh3d parser). Pure TypeScript, runs on Node and GJS via gjsify.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit --project tsconfig.json"
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "fast-xml-parser": "^5.9.3",
    "fflate": "^0.8.2"
  }
}
