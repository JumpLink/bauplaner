/**
 * `@bauplaner/diagnose` — rule-based building diagnostics.
 *
 * Pure TypeScript (no I/O), runs on Node and GJS via gjsify. First module:
 * damp-wall cause screening (`feuchte`).
 */

export * from './feuchte.ts';
