{
  "name": "@bauplaner/diagnose",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Rule-based building diagnostics (damp-wall cause screening). Pure TypeScript, runs on Node and GJS via gjsify.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit --project tsconfig.json"
  },
  "files": [
    "src"
  ]
}
